# AutonomOS Backend Dependencies - Optimized
# Install with: pip install -r requirements-optimized.txt

# Core Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# Performance Enhancements
uvloop>=0.19.0              # 2-4x faster event loop
orjson>=3.9.0               # 2-3x faster JSON parsing
aiocache>=0.12.0            # Async caching framework
redis[hiredis]>=5.0.0       # Fast Redis client with C parser

# HTTP & API
httpx>=0.25.0               # Async HTTP client
h2>=4.1.0                   # HTTP/2 support

# Security & Rate Limiting
slowapi>=0.1.9              # Rate limiting
bleach>=6.1.0               # HTML sanitization
python-jose[cryptography]>=3.3.0  # JWT tokens
passlib[bcrypt]>=1.7.4      # Password hashing

# Execution Engine
croniter>=2.0.1             # Cron scheduling
watchdog>=3.0.0             # File system monitoring

# Monitoring & Logging
prometheus-client>=0.19.0   # Metrics export
sentry-sdk[fastapi]>=1.40.0 # Error tracking
structlog>=24.1.0           # Structured logging

# Database (Optional)
sqlalchemy[asyncio]>=2.0.0  # ORM with async support
aiosqlite>=0.19.0           # Async SQLite
alembic>=1.13.0             # Database migrations

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
httpx-mock>=0.10.0

# Development
black>=23.0.0               # Code formatting
ruff>=0.1.0                 # Fast linting
mypy>=1.7.0                 # Type checking
