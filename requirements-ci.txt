# Lightweight CI requirements - fast install, no heavy ML packages
# Heavy packages (sentence-transformers/torch ~2GB, chromadb, playwright)
# are NOT installed here - they were causing install timeouts and OOM
# in GitHub Actions. Install the full requirements.txt for local dev.

# Core Web Framework
fastapi==0.109.2
uvicorn==0.27.1
pydantic==2.6.1
pydantic-settings==2.1.0

# Scheduling
apscheduler==3.10.4

# HTTP / Async
httpx==0.26.0
aiofiles==23.2.1

# Utilities
python-dotenv==1.0.1
loguru==0.7.2
rich==13.7.0
click==8.1.7

# Security
cryptography==42.0.2
pyjwt==2.8.0

# Performance (lightweight)
orjson==3.9.15

# Testing
pytest==7.4.4
pytest-asyncio==0.23.5
pytest-cov==4.1.0
pytest-mock==3.12.0

# Code Quality
ruff==0.2.1
mypy==1.8.0
